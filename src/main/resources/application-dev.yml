# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_garbage_dev?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 666666
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# 开发环境允许跨域
cors:
  allowed-origins: "*"
  allowed-methods: "*"
  allowed-headers: "*"
  allow-credentials: true

# 开发环境日志级别
logging:
  level:
    com.zjfc.smartgarbage: DEBUG
    org.springframework.web: DEBUG